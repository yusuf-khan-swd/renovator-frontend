export enum ENUM_BOOKING_STATUS {
  PENDING = "pending",
  CANCEL = "canceled",
  ACCEPT = "accepted",
  REJECT = "rejected",
}

export enum ENUM_BOOKING_STATUS_FOR_USER {
  PENDING = "pending",
  CANCEL = "canceled",
}

export enum ENUM_BOOKING_STATUS_FOR_ADMIN {
  ACCEPT = "accepted",
  REJECT = "rejected",
}
