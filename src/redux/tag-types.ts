export enum tagTypes {
  profile = "profile",
  user = "user",
  service = "service",
  admin = "admin",
}

export const tagTypesList = [
  tagTypes.profile,
  tagTypes.user,
  tagTypes.service,
  tagTypes.admin,
];
