export enum tagTypes {
  profile = "profile",
  user = "user",
  service = "service",
  booking = "booking",
  admin = "admin",
}

export const tagTypesList = [
  tagTypes.profile,
  tagTypes.user,
  tagTypes.service,
  tagTypes.admin,
];
